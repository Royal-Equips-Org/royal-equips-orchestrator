# Skip hooks when explicitly requested or on CI
if [ -n "$SKIP_HUSKY" ] || [ -n "$CI" ]; then
  echo "Husky skipped"; exit 0
fi

# Lint only if eslint is available
if command -v pnpm >/dev/null 2>&1 && pnpm -w -s eslint -v >/dev/null 2>&1; then
  pnpm -w eslint . || exit 1
fi

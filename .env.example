# Example environment configuration for Royal Equips Orchestrator

# Shopify credentials
SHOPIFY_API_KEY=your_shopify_api_key
SHOPIFY_API_SECRET=your_shopify_api_secret
# Shopify store subdomain (e.g. my-store if your domain is my-store.myshopify.com)
SHOP_NAME=your_shop_name

# OpenAI API key for customer support agent
OPENAI_API_KEY=your_openai_api_key

# Optional: URL for a PostgreSQL database used by the orchestrator for
# persistent state (not currently used but reserved for future features)
# DATABASE_URL=postgresql://user:password@host:port/dbname

# Optional: SMTP settings for marketing campaigns (if not using Shopify Email)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your_smtp_username
# SMTP_PASSWORD=your_smtp_password
# SMTP_FROM=your_from_address@example.com

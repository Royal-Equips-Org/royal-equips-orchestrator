name: 🛍️ Shopify Integration

on:
  push:
    branches: [main, master]
    paths:
      - 'packages/shopify-client/**'
      - 'packages/connectors/**'
      - 'apps/api/src/v1/shopify.ts'
  schedule:
    - cron: '0 */6 * * *' # Every 6 hours for sync validation
  workflow_dispatch:
    inputs:
      sync_type:
        description: 'Type of synchronization'
        required: true
        default: 'incremental'
        type: choice
        options:
          - incremental
          - full
          - validate

permissions:
  contents: read

env:
  NODE_VERSION: "20"
  PNPM_VERSION: "10.17.0"

jobs:
  shopify-sync:
    name: 🔄 Shopify Data Sync
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4

      - name: 🔧 Setup Environment
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: 📦 Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: 🏗️ Build Shopify Packages
        run: |
          pnpm install --frozen-lockfile --ignore-scripts
          pnpm --filter @royal-equips/shopify-client build
          pnpm --filter @royal-equips/connectors build
        env:
          HUSKY: 0

      - name: 🧪 Test Shopify Integration
        run: |
          # Test Shopify client connectivity
          node -e "
          const { ShopifyConnector } = require('./packages/connectors/dist/shopify.js');
          console.log('Testing Shopify connection...');
          // Add connection test logic
          "
        env:
          SHOPIFY_API_KEY: ${{ secrets.SHOPIFY_API_KEY }}
          SHOPIFY_API_SECRET: ${{ secrets.SHOPIFY_API_SECRET }}
          SHOP_NAME: ${{ secrets.SHOP_NAME }}

      - name: 📊 Sync Validation
        run: |
          echo "Sync Type: ${{ github.event.inputs.sync_type || 'scheduled' }}"
          # Validate inventory sync
          # Validate order sync
          # Validate customer sync
          # Validate product sync
          echo "Shopify integration validation complete"

      - name: 📢 Report Status
        if: always()
        run: |
          echo "Shopify Integration Status: ${{ job.status }}"
          # Could integrate with Slack/Discord for notifications
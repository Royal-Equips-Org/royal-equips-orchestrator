# Minimal Flask requirements for lightweight deployments (Render python runtime)
# Excludes heavy scientific libs to prevent build failures
flask>=3.0
flask-cors>=6.0
gunicorn>=21.2
requests>=2.31
python-dotenv>=1.0
jinja2>=3.1

# Basic testing support
pytest>=8.0